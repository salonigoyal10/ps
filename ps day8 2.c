#include<stdio.h>
#include<math.h>
int main()
{
    int a,b,x,y,c=1,k;
    printf("enter the values of a,b");
    scanf("%d%d",&a,&b,)
    x=pow(a,b);
    while(c!=k)
    {
        x=x/10;
        c++;
    }
    y=x%10;
    printf("%d",y);
}

